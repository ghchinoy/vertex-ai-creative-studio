# Python-generated files
__pycache__/
**/__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv/
**/.venv/

# The repo-root .gitignore ignores all `bin/` dirs; re-include ours so
# bin/PROVENANCE.md is tracked, but keep the built binary itself out (large,
# platform-specific, PoC-only). scripts/build_credentio.sh rebuilds it.
!bin/
bin/c2pa_validate

# Local scratch / downloaded extras
*.log
